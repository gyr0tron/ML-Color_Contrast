{
  "name": "ml-color_contrast",
  "version": "1.0.0",
  "description": "Sets color of text (b/w) with respect to the background",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "webpack-dev-server --mode development --entry ./src/js/app.js --output-filename ./dist/bundle.js",
    "build:prod": "webpack --mode production -p"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "webpack": "^4.2.0",
    "webpack-cli": "^2.0.12",
    "webpack-dev-server": "^3.1.1"
  }
}
